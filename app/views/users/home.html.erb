<div class="home">
<div class="container">
    <div class="row mt-md-5">
        <div class="col-md-8 p-3">
            <h1>Discover designers and devs.<br />
            Learn and share skills.</h1>
            <p class="pt-3">Cocobuds is a platform that helps you connect with designers and developers in your area. It's a great way to learn and share skills while expanding your network.</p>
            <%= button_to("Get started", "#search", {method: :get, class: "btn btn-primary mt-3"})%>
        </div>
        <div class="col-md-4">
        </div>
    </div>

        <div class="row">
            <div class="col-md-6 p-3 text-right">
            </div>
            <div class="col-md-6 p-3">
                <%= image_tag "cocobuds_landing_page_friends.jpg", class:"img-fluid" %>
            </div>
        </div>

        <div class="row mt-md-5">
            <div class="col-md-3 p-3 text-right">
                <h2>It's not</h2>
            </div>
            <div class="col-md-7 p-3 mt-4 text-right">
                <h2>just what it looks like<br /> and feels like.</h2>
                <h2 class="mt-md-4">Design is how it works.</h2>
                <p>Steve Jobs</p>
            </div>
            <div class="col-md-2 p-3">
            </div>
        </div>


  <div class="row landing justify-content-between pt-5" id="search">
    <div class="col-md-5 p-3 pt-5 mt-md-5">
      <h2 class="mb-4">Find a Cocobud</h2>
      <%= form_with url: preview_path, method: :get, local: true do |form| %>
        <div class="form-group">
            <%= form.label :location, "Pick a location", class: "sr-only" %>
            <%= form.select(:location, options_for_select( @locations, params[:location] ), {include_blank: 'Pick a Location'}, {required: true, class: "form-control form-control-md"}) %>
        </div>
        <div class="form-group">
            <%= form.label :role, "Looking for a developer or a designer?", class: "sr-only" %>
            <%= form.select(:role, options_for_select( @roles, params[:role] ), {include_blank: 'Find a developer or a designer?'}, {required: true, class: "form-control form-control-md"}) %>
        </div>
        <div class="form-group">
            <%= form.label :skill, "What skills do you have?", class: "sr-only" %>
            <%= form.select(:skill, options_for_select( @skills.pluck(:name), params[:skill] ), {include_blank: 'Select a Skill'}, {required: true, class: "form-control form-control-md"}) %>
        </div>
        <div class="mb-3 form-group text-center">
        <%= form.submit 'Search', class: "mt-3 w-100 btn btn-primary btn-blocks" %>
        </div>
      <% end %>
    </div>
    <div class="col-md-6 p-3 pt-5 mt-md-5 mr-n2">
        <%= image_tag "search_friends.svg", class:"img-fluid" %>
    </div>
</div>
</div>

